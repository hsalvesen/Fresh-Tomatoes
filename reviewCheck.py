# Checks if there are any reviews
def reviewCheck(reviews) :
    # If there is any text in review variable
    if len(reviews) > 0 :
        return True
    # There are no reviews
    else :
        return False
